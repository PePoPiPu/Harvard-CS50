#include <cs50.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char rotate (char c, int n);

int main (int argc, string argv[])
{

char rotate (char c, int n)
{
    for (int i = 0; length = strlen(s); i < length; i++)
    {
        
    }
}
}