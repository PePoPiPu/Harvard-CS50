#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[])
{
    // Ensure proper usage
    if (argc < 2)
        printf("Usage: ./hash WORD\n")

    // For every commad-line argument
    for (int argv)
}