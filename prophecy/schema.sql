CREATE TABLE student(
    student_id INTEGER,
    student_name TEXT,
    PRIMARY KEY(student_id)
);

CREATE TABLE house (
    house_id INTEGER,
    house_name TEXT,
    PRIMARY KEY(house_name)
);

CREATE TABLE head (
    head_id INTEGER,
    head_name TEXT,
    PRIMARY KEY(head_name)
);
